name: Build and push to harbor

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure Docker for insecure registry
        run: |
          sudo mkdir -p /etc/docker
          echo '{"insecure-registries": ["harbor.scanmypassport.de"]}' | sudo tee /etc/docker/daemon.json
          sudo systemctl restart docker
          echo "Docker daemon configured to allow insecure registry: harbor.scanmypassport.de"

      - name: Login to Harbor
        run: |
          echo "${{ secrets.HARBOR_PASSWORD }}" | docker login harbor.scanmypassport.de \
            --username admin --password-stdin

      - name: Build image
        run: docker build -t harbor.scanmypassport.de/test/hexagonal:latest .

      - name: Push image
        run: docker push harbor.scanmypassport.de/test/hexagonal:latest